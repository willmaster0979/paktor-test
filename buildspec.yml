version: 0.2

phases:
  install:
    commands:
      - npm install -g serverless
  build:
    commands:
      - serverless deploy | tee deploy.out
  #post_build:
  #  commands:

#artifacts:
#  files:
#    - lambda_function.py
  #discard-paths: yes
  #base-directory: target
