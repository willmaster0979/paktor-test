version: 0.2

phases:
  #install:
  #  commands:
  #    - pip install pyyaml
  build:
    commands:
      - echo Build started on `date`
      #- echo Compiling the Python code......
      - "cp src/lambda_function.py target/"
      - "cd target && zip lambda_function.zip lambda_function.py"
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    files:
      - lambda_function.zip
    discard-paths: "yes"
    base-directory: "target"